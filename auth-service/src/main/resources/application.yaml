spring:
  application:
    name: auth-service
  config:
    import: configserver:http://localhost:8888

server:
  port: 8081

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics

  endpoint:
    health:
      show-details: always

info:
  app:
    name: Auth Service
    version: 1.0.0